{
  "name": "serverless-app",
  "version": "1.0.0",
  "description": "Serverless Node.js app deployed with Serverless Framework",
  "main": "handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "test": "echo \"No test specified yet\" && exit 1"
  },
  "keywords": ["serverless", "aws", "nodejs"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.812.0",
    "@aws-sdk/client-lambda": "^3.812.0",
    "@aws-sdk/client-s3": "^3.812.0"
  },
  "devDependencies": {
    "serverless": "^3.39.0"
  }
}
